import 'package:flutter/material.dart';

void main() => runApp(MaterialApp(
      home: FocusPlanner(),
    ));

class FocusPlanner extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
